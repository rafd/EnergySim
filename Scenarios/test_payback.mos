runScript("_sim.mos");
getErrorString();

loadModel(Modelica.Blocks);
loadFile("test_payback.mo");
getErrorString();

simulate(Sys, startTime=0*6*30*86400, stopTime=10,outputFormat="plt");

getErrorString();

plot(PayBackPeriod);

getErrorString();
